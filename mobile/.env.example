# Point the app at your running server. localhost only works in the iOS
# Simulator / Android emulator / web — use your computer's LAN IP for a
# physical device, e.g. EXPO_PUBLIC_API_URL=http://192.168.1.20:4000
EXPO_PUBLIC_API_URL=http://localhost:4000
